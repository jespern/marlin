# Optional assets

`mk.pak` and `wo.pak` are downloaded on demand and are not embedded in Marlin.
Their existing container formats are unchanged.

See [asset maintenance](../docs/ASSETS.md) for validation, regeneration and release
instructions. Keep the recipes and expected digests in sync with these files.
