# Downloadable Mario Kart assets

These content-addressed bundles are served through raw GitHub. They are not
embedded in the Marlin executable. Keep existing hash-named files available
when publishing updates so older clients can still download their assets.

See [regeneration and publishing instructions](../../docs/MK64_ASSETS.md).
The expected digest and download URL live in `src/mk64/cache.zig`.
