{
  "steps": [
    {
      "expect_contains": ["trigger an error"],
      "status": 500,
      "body": "{\"error\":{\"message\":\"synthetic upstream explosion\",\"code\":500}}"
    }
  ],
  "check": {
    "argv": ["run", "--quiet", "--model", "local/testing", "trigger an error"],
    "exit_code": 1,
    "stderr_contains": ["turn failed"],
    "db_kinds": ["user_msg", "system_note"]
  }
}
